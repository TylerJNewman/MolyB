<%= form_for(@api_note) do |f| %>
  <% if @api_note.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@api_note.errors.count, "error") %> prohibited this api_note from being saved:</h2>

      <ul>
      <% @api_note.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
