<%= form_for(@api_notebook) do |f| %>
  <% if @api_notebook.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@api_notebook.errors.count, "error") %> prohibited this api_notebook from being saved:</h2>

      <ul>
      <% @api_notebook.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
